<main class="token-thank-you container mx-auto py-12">
  <div class="card card--hero mx-auto">
    <div class="card__icon">
      <i class="fa fa-envelope fa-3x"></i>
    </div>
    <h1 class="card__title">Check Your Inbox</h1>
    <p class="card__text">
      We’ve sent you an email with a secure login link to manage your 3D print jobs.
      Please check your inbox—and if you don’t see it, your spam folder.
    </p>
    <div class="card__actions">
      <%= link_to 'Resend Login Link', login_path, class: 'btn btn-outline' %>
      <%= link_to 'Submit a Print', submit_print_path, class: 'btn btn-primary' %>
      <% if scanning_available? %>
        <%= link_to 'Submit a Scan', submit_scan_path, class: 'btn btn-secondary' %>
      <% end %>
    </div>
  </div>
</main>
